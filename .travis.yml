---
install:
- sudo apt-get update -y
- sudo apt-get install hugo -y

script:
- hugo

deploy:
- provider: pages
  skip-cleanup: true
  local-dir: public
  github-token: $GITHUB_TOKEN
  keep-history: false
